import { useEffect, useState } from "react";
import "./Gallery.css";

export const Gallery = ({ data }) => {
  const [dataPage, setDataPage] = useState(null);

  useEffect(() => {}, []);

  return <div id="galleryRicky"></div>;
};
